import { Heading } from "@chakra-ui/react";
import React from "react";

function Information(props: any) {
  const { data } = props;
  return console.log(data.class_1);
}

export default Information;
